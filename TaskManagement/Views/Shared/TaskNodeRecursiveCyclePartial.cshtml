@model List<TaskManagement.Models.TaskNode> 

@foreach (TaskNode taskNode in Model)
{
    @if (taskNode.Parent != null)
        continue;

    @await Html.PartialAsync("TaskNodeRecursivePartial", taskNode)
}
